{
  "name": "create_timer",
  "description": "Create a timer.",
  "parameters": {
    "type": "object",
    "properties": {
      "duration": {
        "type": "integer",
        "description": "The duration of the timer in seconds."
      }
    },
    "required": ["duration"]
  }
}
